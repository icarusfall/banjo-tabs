# Postgres connection string (Railway provides this automatically when you attach a DB).
DATABASE_URL=postgres://user:pass@localhost:5432/banjo

# Optional: port (Railway sets this automatically).
PORT=5174
